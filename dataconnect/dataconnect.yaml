specVersion: "v1beta"
serviceId: "chalana-chitram"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chalana-chitram-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
